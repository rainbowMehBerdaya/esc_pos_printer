name: esc_pos_printer
publish_to: none
description: The library allows to print receipts using an ESC/POS thermal WiFi printer.
version: 4.1.0
homepage: https://github.com/andrey-ushakov/esc_pos_printer

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  charset_converter: ^2.1.1
#  esc_pos_utils: ^1.1.0
  esc_pos_utils:
    git:
      url: https://github.com/rainbowMehBerdaya/esc_pos_utils.git
      ref: 6824eeb44a61d9e4ecacf431cc747b885b3128f9
  flutter:
    sdk: flutter
  # esc_pos_utils:
  #   path: ../esc_pos_utils
  image: ^4.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
